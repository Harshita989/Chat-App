<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit the form</title>
</head>
<body>
    <h3>Edit the form:</h3>
    <p>ID : <%= chats.id %></p>
    <p>This message was send by <b>  <%= chats.from %></b> to <b> <%= chats.to %></b></p>
    <form method="post" action="/chats/<%= chats._id %>?_method=PUT">
        <textarea name="msg" id="" rows="5" cols="30"> <%= chats.msg %></textarea><br>
    <button> Edit</button>
    
    </form>
    
</body>
</html>